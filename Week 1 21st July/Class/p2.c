#include <stdio.h>
#include <stdlib.h>
#include <time.h>
void main(){

    int i,n;
    int *p;
    time_t t;
    srand(())
    printf("Enter size of data:");
    scanf(%d,&);
    p=(int*)malloc(n*sizeof(int));
    for(i=0;i<0;i++){
        *(p+i)=rand()%100;
    }
    
}